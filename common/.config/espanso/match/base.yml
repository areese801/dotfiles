# Espanso Text Expansions
# Migrated from Alfred snippets on 2026-04-09
# Trigger prefix: ~!
#
# espanso docs: https://espanso.org/docs/matches/basics/

matches:

  # ============================================================================
  # Date / Time
  # ============================================================================

  - trigger: "~!d"
    replace: "{{date}}"
    vars:
      - name: date
        type: date
        params:
          format: "%Y-%m-%d"

  # ============================================================================
  # Personal
  # ============================================================================

  - trigger: "~!e"
    replace: "areese801@gmail.com"

  - trigger: "~!li"
    replace: "https://www.linkedin.com/in/areese801/"

  - trigger: "~!gh"
    replace: "https://github.com/areese801"

  # ============================================================================
  # Claude / LLM Prompting
  # ============================================================================

  - trigger: "~!ask"
    replace: "Ask clarifying questions as needed."

  # ============================================================================
  # SQL
  # ============================================================================

  - trigger: "~!S"
    replace: "SELECT\n\tt.*\nFROM\n\t{{clip}} t\n-- WHERE\n\nLIMIT 100;"
    vars:
      - name: clip
        type: shell
        params:
          cmd: "{ pbpaste 2>/dev/null || wl-paste 2>/dev/null || xclip -selection clipboard -o 2>/dev/null; } | tr '[:upper:]' '[:lower:]'"

  - trigger: "~!mbasgb"
    replace: "megabytes as gigabytes"

  - trigger: "~!sah"
    replace: "seconds as hours"

  # ============================================================================
  # Snowflake
  # ============================================================================

  - trigger: "~!u"
    replace: "SHOW DATABASES;\nUSE DATABASE foo;\nSHOW SCHEMAS;\nUSE SCHEMA bar;\nSHOW OBJECTS;"

  # ============================================================================
  # dbt
  # ============================================================================

  - trigger: "~!ref"
    replace: "{{ ref('') }}"

  - trigger: "~!source"
    replace: "{{ source('<source_name>', '<table_name>') }}"

  - trigger: "~!compile"
    replace: "dbt compile --select {{clip}}"
    vars:
      - name: clip
        type: shell
        params:
          cmd: "{ pbpaste 2>/dev/null || wl-paste 2>/dev/null || xclip -selection clipboard -o 2>/dev/null; } | tr '[:upper:]' '[:lower:]'"

  # ============================================================================
  # Jira
  # ============================================================================

  - trigger: "~!j"
    replace: |
      # Stakeholders
      List all Stakeholders Here. (@-tagging them as it makes sense)

      # Core Problem Statement

      Describe the problem that this card addresses here in a few sentences. Think:  TL;DR

      # Details
      Put more details here. This section complements the Core Problem Statement (TL;DR) section above

      # See Also:
      Use this section to link out to other artifacts, conversations, documents, lucid charts, slack channels, etc.

  # ============================================================================
  # Gmail
  # ============================================================================

  - trigger: "~!from"
    replace: "-label:chat from:"

  # ============================================================================
  # Regex Patterns
  # ============================================================================

  # Find single-line commas (for find/replace: reformat to leading commas)
  - trigger: "~!re,f"
    replace: "((?<!\\n), )"

  # Replace single-line commas with leading commas
  - trigger: "~!re,r"
    replace: "\\n\\t, "

  # Match underscore, dash, or space (useful for fuzzy name matching)
  - trigger: "~!_"
    replace: "(\\ |_|-)?"

  # Negative match pattern (exclude lines containing a word)
  - trigger: "~!notmatch"
    replace: "^((?!badword).)*$"

  # ============================================================================
  # Shell
  # ============================================================================

  # Pipe SnowSQL output to clean TSV
  - trigger: "~!sedtsv"
    replace: " | grep -v -E '^$' | grep -E '\\s*[\\|\\+]' | grep -v -E '^(\\+|\\|)[+-]+(\\+|\\|)$' | grep -v -E '^[+ -]+$' | sed 's/^[[:space:]]*|[[:space:]]//g' | sed 's/[[:space:]]*|[[:space:]]*$//g' | sed 's/[[:space:]]*|[[:space:]]*/\\t/g'"

  # ============================================================================
  # Private Snippets (fetched from private snippets repo at expansion time)
  # ============================================================================
  # Pattern: trigger expands by shelling out to the private snippets repo and
  # printing the snippet body. Only the UUID lives in this public config — the
  # snippet content stays in the private repo at ~/projects/personal/snippets.

  - trigger: "~!cust"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 42536120-8043-4112-b7fa-2cdffa47d0c2 --print"

  - trigger: "~!transactions"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 6faa1a98-8c6c-4bda-9a4f-6ec66fc28f4a --print"

  - trigger: "~!integration"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get c2fca6c4-e4fb-45f4-aa18-fec6c26c9af9 --print"

  - trigger: "~!targets"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 010888b4-62c3-4c5a-99ee-aa94096276d8 --print"

  - trigger: "~!company"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 695e329e-0d09-4124-9b41-18853fa1108d --print"

  - trigger: "~!cred"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 3774f403-9331-47a0-8bd5-6d0d590bddbc --print"

  - trigger: "~!columns"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get c3242223-db65-4a0e-927a-374a2ad1f3ba --print"

  # --- QA queries (confirm records landed during ETL loads) ---

  - trigger: "~!qaloc"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get a827dd84-5c33-44f3-ba3f-735603eed3f5 --print"

  - trigger: "~!qacust"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 021c7c64-910b-49ae-9ce9-f158e2f9dea3 --print"

  - trigger: "~!qasub"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 849fef74-4ba0-4a29-ade5-8b5b122bb191 --print"

  - trigger: "~!qatrans"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get a1b02590-7f01-4e0c-8658-e57ca37ba594 --print"

  - trigger: "~!qavehicle"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 28c5a637-1590-428e-85cd-6037ae93d85d --print"

  - trigger: "~!qarealestate"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 21b54727-6eee-4fa6-9d1e-78923665306e --print"

  - trigger: "~!qasched"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get c0b36087-10be-4d58-8dba-311a25a21605 --print"

  - trigger: "~!qalog"
    replace: "{{snippet}}"
    vars:
      - name: snippet
        type: shell
        params:
          cmd: "$HOME/projects/personal/snippets/get 91a4413c-6eb1-4f1d-984b-7f998b8849ff --print"
